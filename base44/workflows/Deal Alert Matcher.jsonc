{
  "name": "Deal Alert Matcher",
  "description": "When a property is updated to active status (promoted from draft or newly listed), match it to investor saved searches and create alerts + send emails. Deduped per property+user.",
  "trigger": {
    "config": {
      "trigger_type": "entity",
      "entity_name": "Property",
      "events": [
        "update"
      ]
    },
    "condition": "${ .trigger.data.status == \"active\" }"
  },
  "definition": {
    "document": {
      "dsl": "1.0.0",
      "name": "deal_alert_matcher",
      "version": "1.0",
      "namespace": "base44"
    },
    "do": [
      {
        "match_alerts": {
          "call": "invoke_backend_function",
          "with": {
            "function_name": "matchAndNotifyAlerts",
            "args": {
              "property_id": "${ .trigger.entity_id }"
            }
          },
          "then": "end",
          "x-base44": {
            "title": "Match property to saved searches",
            "description": "Create alerts and send emails for newly-visible properties"
          }
        }
      }
    ]
  }
}